server:
  port: 8769
spring:
  application:
    name: service-zuul
  cloud:
      config:
          label: master
          profile: dev
          uri: http://localhost:8770/
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh